<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_dashboard"
            android:tint="@color/white"
            android:title="@string/menu_home" />
        <item
            android:id="@+id/nav_camera"
            android:icon="@drawable/floating_camera"
            android:tint="@color/white"
            android:title="@string/menu_ra" />

        <item
            android:id="@+id/nav_maps"
            android:icon="@drawable/ic_menu_slideshow"
            android:tint="@color/white"
            android:title="@string/menu_maps" />

        <item
            android:id="@+id/nav_takeme"
            android:icon="@drawable/ic_menu_slideshow"
            android:tint="@color/white"
            android:title="Llévame" />

    </group>

    <item android:title="@string/menu_accessibility">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:tint="@color/white"
                android:title="@string/menu_share" />
            <item
                android:id="@+id/nav_accessibility"
                android:icon="@drawable/ic_menu_share"
                android:tint="@color/white"
                android:title="Modalidad 2" />

            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_menu_send"
                android:tint="@color/white"
                android:title="@string/menu_settings" />
        </menu>
    </item>

</menu>
